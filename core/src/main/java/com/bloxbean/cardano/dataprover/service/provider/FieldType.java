package com.bloxbean.cardano.dataprover.service.provider;

/**
 * Supported field types for provider configuration schema.
 * Used by the UI to render appropriate input controls.
 */
public enum FieldType {
    STRING,
    INTEGER,
    NUMBER,
    BOOLEAN,
    SELECT,
    PASSWORD
}
